import { styled } from "@mui/system";
import theme from "../../theme";

const ParargraphText = styled("p")({
  fontFamily: theme.typography.h1.fontFamily,
  fontSize: 18,
});

export default ParargraphText;
